data = str(135333)
# print(data[0:3])
half = int(len(data)/2)
back = data[half:]
front = data[:half]

bresult = 0
fresult = 0

for i in range(len(back)):
  for j in range(len(front)):
    bresult += int(back[i])
    fresult += int(front[i])

if bresult == fresult:
  print('LUCKY')
else:
  print('READY')
